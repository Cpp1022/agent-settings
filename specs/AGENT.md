# AGENT Entry (Token-Light)

Keep this file short. Use it as an index only.

## Load Order

1. `specs/RULES_INDEX.md`
2. files listed there, in order

## Policy

- Prefer short rules in indexed files.
- If a rule text is longer than 180 characters, move it to a referenced file.
- Do not paste long command examples here.
- For shell approvals, always request a reusable `prefix_rule` when safe.
